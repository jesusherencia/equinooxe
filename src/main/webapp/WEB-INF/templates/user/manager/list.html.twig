{% extends "../../layout" %}

{% block title %}
  Liste des managers
{% endblock %}

{% block content %}
		<div class="titre">
			<i class="ti-list-ol"></i> <span> Liste des managers </span>
			<div class="dropdown pull-right">
	             <a href="#" class="dropdown-toggle card-drop" title="Options" data-toggle="dropdown" aria-expanded="false">
	                 <i style="font-size: 18px" 
	                    class="ti-settings"></i>
	            </a>
	            <ul class="dropdown-menu" role="menu">
	               <li>
	                  <a href="/user/new">Nouvel Aganet</a>
	                  <a href="/user/manager/new">Nouveau Manager</a>
	               </li>
	            </ul>
	        </div>
		</div>
		
		<table class="table">
			<thead>
				<tr>
					<th>Nom</th>
					<th>Email</th>
					<th>Roles</th>
					<th>Actif</th>
					<th></th>
				</tr>
			</thead>
			<tbody>
			    {% for userEntry in users %}
				<tr>
					<td> {{ userEntry.firstName }} {{ userEntry.lastName }}</td>
					<td> {{ userEntry.email }}</td>
					<td>
					     {% for auth in userEntry.authorities %}
						     {{ auth.name }} 
						 {% endfor %}
                    </td>
					<td>{{ userEntry.activated }}</td>
					<td>
					   <div class="dropdown pull-right">
                             <a href="#" class="dropdown-toggle card-drop" data-toggle="dropdown" aria-expanded="false">
                                  <i class="zmdi zmdi-more-vert"></i>
                             </a>
                            <ul class="dropdown-menu" role="menu">
                                 <li> <a href="{{ href('/user/manager/show')}}/?id={{ userEntry.id }}" >Détails</a></li>
                                 <li> <a href="{{ href('/user/manager/edit')}}/?id={{ userEntry.id }}" >Editer </a></li>
                            </ul>
                       </div>
					</td>
				</tr>
				{% endfor %}
			</tbody>
		</table>
{% endblock %}